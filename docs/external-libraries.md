OPENCV 4.13.0           -> https://github.com/opencv/opencv/releases/tag/4.13.0
ONNXRuntime 1.24.1      -> https://github.com/microsoft/onnxruntime/releases/tag/v1.24.1


DLL's are to be put in the output directory for the built .exe.
- x64/Debug  or /Release depending on build type.

Screenhots for linking setup is provided. I don't know how obvious stuff like this is but I have it for my own ref lol.

Debug version of opencv is larger than 100mb. This means I cannot upload it to github.
Either only use the release build from now, or, download the Windows .exe release from the opencv github, and manually
do that. 

This will most likely have to be done anyway to be able to compile the build.

- Oliwier Kulczycki

